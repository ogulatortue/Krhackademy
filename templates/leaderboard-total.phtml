<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Classement - Kr[HACK]ademy</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/content-page.css">
    <link rel="stylesheet" href="/css/leaderboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"/>
</head>
<body>

    <main class="content-container">
        <div class="content-wrapper">

            <div class="content-header">
                <i class="content-icon fas fa-trophy"></i>
                <div class="content-title-group">
                    <h1>Classement</h1>
                    <p class="content-intro">Mesurez-vous à la communauté. Chaque drapeau capturé vous rapproche du sommet !</p>
                </div>
            </div>

            <div class="content-block">

                <?php 
                    $podiumUsers = array_slice($leaderboardData, 0, 3);
                    $otherUsers = array_slice($leaderboardData, 3);
                ?>

                <div class="podium-container">
                    <?php if (isset($podiumUsers[1])): $user = $podiumUsers[1]; ?>
                        <div class="podium-card podium-card--2 <?php echo ($user['id'] == $currentUserId) ? 'podium-card--current-user' : ''; ?>">
                            <div class="podium-rank">2</div>
                            <i class="podium-icon fas fa-medal"></i>
                            <div class="podium-user"><?php echo htmlspecialchars($user['username']); ?></div>
                            <div class="podium-score"><?php echo $user['score']; ?> pts</div>
                        </div>
                    <?php endif; ?>

                    <?php if (isset($podiumUsers[0])): $user = $podiumUsers[0]; ?>
                        <div class="podium-card podium-card--1 <?php echo ($user['id'] == $currentUserId) ? 'podium-card--current-user' : ''; ?>">
                            <div class="podium-rank">1</div>
                            <i class="podium-icon fas fa-trophy"></i>
                            <div class="podium-user"><?php echo htmlspecialchars($user['username']); ?></div>
                            <div class="podium-score"><?php echo $user['score']; ?> pts</div>
                        </div>
                    <?php endif; ?>

                    <?php if (isset($podiumUsers[2])): $user = $podiumUsers[2]; ?>
                        <div class="podium-card podium-card--3 <?php echo ($user['id'] == $currentUserId) ? 'podium-card--current-user' : ''; ?>">
                            <div class="podium-rank">3</div>
                            <i class="podium-icon fas fa-medal"></i>
                            <div class="podium-user"><?php echo htmlspecialchars($user['username']); ?></div>
                            <div class="podium-score"><?php echo $user['score']; ?> pts</div>
                        </div>
                    <?php endif; ?>
                </div>
            </div>

            <div class="content-block">
                <div class="leaderboard-table">
                    <div class="leaderboard-header">
                        <span class="leaderboard-cell">Rang</span>
                        <span class="leaderboard-cell">Utilisateur</span>
                        <span class="leaderboard-cell">Score</span>
                    </div>

                    <?php foreach ($otherUsers as $index => $user): ?>
                        <?php
                            // Le rang réel est l'index dans cette nouvelle boucle + 4 (car on a retiré les 3 premiers)
                            $rank = $index + 4;
                            $isCurrentUser = ($user['id'] == $currentUserId);
                        ?>
                        <div class="leaderboard-row <?php echo $isCurrentUser ? 'leaderboard-row--current-user' : ''; ?>">
                            <span class="leaderboard-cell leaderboard-cell--rank">#<?php echo $rank; ?></span>
                            <span class="leaderboard-cell leaderboard-cell--user"><?php echo htmlspecialchars($user['username']); ?></span>
                            <span class="leaderboard-cell leaderboard-cell--score"><?php echo $user['score']; ?> pts</span>
                        </div>
                    <?php endforeach; ?>
                </div>

            </div>
        </div>
    </main>
</body>
</html>