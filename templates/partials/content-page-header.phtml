<?php
    $isLesson = ($type === 'lesson');
    $difficulty = $isLesson ? $item['difficulty'] : $item['points'];
    $icon = $item['icon_class'] ?? ($isLesson ? 'fa-book-open' : 'fa-flag');
    $pointsDisplay = $isLesson ? '' : htmlspecialchars($item['points']) . ' <i class="fas fa-flag"></i>';
?>
<header class="content-header">
    <i class="content-icon fas <?= htmlspecialchars($icon) ?>"></i>
    <div class="content-title-group">
        <h1><?= htmlspecialchars($item['title']) ?></h1>
        <?php if ($isLesson): ?>
            <p class="content-intro"><?= htmlspecialchars($item['description']) ?></p>
        <?php endif; ?>
    </div>
    <span class="difficulty-tag <?= getDifficultyClass($difficulty) ?>">
        <?= $isLesson ? htmlspecialchars($difficulty) : $pointsDisplay ?>
    </span>
</header>