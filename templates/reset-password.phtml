<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Nouveau mot de passe - Kr[HACK]ademy</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"/>
</head>
<body class="auth-page">

    <a href="/" class="btn-glass back-button"><i class="fas fa-arrow-left" aria-hidden="true"></i>Retour à l'accueil</a>

    <div class="auth-container">
        <div class="auth-content">
            <h1 class="site-title">Kr[HACK]ademy</h1>
            <h2 class="page-title">Choisissez un nouveau mot de passe</h2>
            
            <?php if (!empty($error_message)): ?>
                <p class="error-message"><?= htmlspecialchars($error_message) ?></p>
            <?php elseif (!empty($success_message)): ?>
                <p class="success-message"><?= htmlspecialchars($success_message) ?></p>
                <p class="auth-link"><a href="/login">Se connecter</a></p>
            <?php else: ?>
                <form action="/reset-password" method="post" class="auth-form">
                    <input type="hidden" name="token" value="<?= htmlspecialchars($token ?? '') ?>">
                    <input type="password" name="password" placeholder="Nouveau mot de passe" required>
                    <input type="password" name="password_confirm" placeholder="Confirmer le nouveau mot de passe" required>
                    <button type="submit" class="btn-glass">Changer le mot de passe <i class="fas fa-arrow-right" aria-hidden="true"></i></button>
                </form>
            <?php endif; ?>
        </div>
    </div>

</body>
</html>